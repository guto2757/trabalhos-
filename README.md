# trabalhos-
using System;

class Program
{
    static void Main()
    {
        // Variável string declarada
        string nome;

        // Inicializa antes do uso — evita warning
        nome = "Usuário";

        Console.WriteLine($"Olá, {nome}!");
    }
}
